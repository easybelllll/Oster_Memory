<div id="login" class="page">
    <div class="popup">
        <h2>In Rangliste eintragen</h2>
        <p>Mit Name und E-Mail eintragen oder als Gast fortfahren</p>
        <input type="text" id="name" placeholder="Name" />
        <input type="email" id="email" placeholder="E-Mail" />
        <br />
        <button onclick="submitLogin()">Speichern</button>
        <button class="btn-outline-pink" onclick="continueAsGuest()"
            >Als Gast fortfahren</button
        >
    </div>
    <img src="/img/decoration.png" class="login-decoration" />
</div>

<style>
    .page {
        display: none;
        min-height: 100vh;
        justify-content: center;
        align-items: center;
    }
    .page.active {
        display: flex;
    }
    .popup {
        background: white;
        padding: 40px;
        border-radius: 25px;
        max-width: 800px;
        margin: auto;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
        text-align: center;
    }
    button {
        background: #e98989;
        border: none;
        padding: 12px 20px;
        border-radius: 20px;
        margin: 10px;
        cursor: pointer;
        color: white;
        font-size: 16px;
        transition: 0.2s;
    }
    button:hover {
        transform: scale(1.05);
    }
    .btn-outline-pink {
        background: white;
        color: #e98989;
        border: 2px solid #e98989;
    }
    input {
        width: 80%;
        margin: 10px auto;
        padding: 10px;
        border-radius: 8px;
        border: 1px solid #ddd;
        display: block;
    }
    .login-decoration {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: auto;
        pointer-events: none;
        object-fit: cover;
    }
</style>
